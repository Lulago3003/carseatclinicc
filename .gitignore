# Archivos del sistema
.DS_Store
Thumbs.db
desktop.ini

# Dependencias (por si en el futuro se usan)
node_modules/

# Logs y temporales
*.log
.cache/

# Documentos fuente pesados (inventario, etc.) — no van en la web
*.docx

# Archivos pesados que no son parte de la web
*.zip

# Carpetas de trabajo local (PDFs de inventario, etc.) — no van en la web
output/
tmp/

# Versión 2 (laboratorio local) — NO se sube a la web
v2/

# Fotos originales que manda la clienta (fuente, no van a la web)
fotografias profesionales/
